{% extends 'base.html.twig' %}

{% block title %}All category{% endblock %}

{% block body %}
    <h1>Toutes les séries de la table catégorie : </h1>
    {% for category in categories %}
        <div>
            <h2>{{ loop.index }} / {{ category.name }} </h2>
       
            
    <a href="{{ path('category_show', {categoryName: category.name}) }}"</a>

        </div>
    {% endfor %}

    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}

    
